{
  "name": "kubescape-operator",
  "version": "0.1.0",
  "private": false,
  "description": "Pulumi Kubernetes stack: kubescape-operator",
  "type": "module",
  "scripts": {
    "up": "pulumi up --yes --stack ${PULUMI_STACK:-prod}",
    "preview": "pulumi preview --stack ${PULUMI_STACK:-prod}",
    "destroy": "pulumi destroy --yes --stack ${PULUMI_STACK:-prod}"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/kubernetes": "^4.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}